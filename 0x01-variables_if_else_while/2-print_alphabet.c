#include <stdio.h>

/**
 * main-Short description, single line
 * a blank line
 * Description: Longer description of the function)?
 * Return: Always zero
 */

int main(void)
{
	int ch;

	for (ch = 'a'; ch <= 'z'; ch++)
	{
	putchar(ch);
	}
	putchar('\n');

	return (0);
}
